# Review 剩余问题

> Review 进度：首次全量基线审计及本轮无需决策 bug 修复已推进至 `b49c4e3`；审计起点为 `62bbf850b5667091a6bbf21e8d479d26b30e9928`，审计终点为 `55bce3f51aa09b3d72fdd4cf98f129e7bddff0fd`。下次增量 review 从 `b49c4e3` 开始。本次只审查终点提交中的已提交代码，未跟踪的 MCP Portal runbook 未纳入范围。

## 未解决问题

- [R-14] [P1] MCP bearer token 长期以明文保存在 D1，并在管理页面和安装内容中反复输出。
- [R-07] [P2] 图谱 ANN 种子阶段没有按 `recordKind` 过滤，混合 record kind 时可能错失允许范围内的图证据。
- [R-15] [P2] MCP token 生命周期和 `/mcp` 鉴权负路径缺少针对性测试，安全边界缺少回归门禁。
- [R-09] [P3] 离线 eval 没有覆盖 graph channel，图召回和图排序回归不会使 eval 失败。

## 已拍板不改

- [R-05] [P1] hard delete 失败后保留 `purge_pending_at`。复核确认 hard delete 可以
  重新执行 `prepareMemoryPurge` 创建新审计并重试，pending 状态会有意阻止 restore，
  避免删除尚未完成时恢复数据；当前没有确认的锁死 bug，因此不改实现。
