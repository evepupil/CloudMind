# 数据主权与生命周期

> 模块定位：保证原始输入不可变，并让个人数据可完整导出、重建和迁移。
>
> 对应代码：`src/core/{assets,blob,vector}/*`、`src/features/{assets,workflows}/server/*`、`src/platform/{blob,db,vector}/*`、`scripts/ops/*`
>
> 所属 M 里程碑：[`M6 数据主权与生命周期`](../roadmap.md#m6-数据主权与生命周期)
>
> 当前状态：已完成
>
> 最近更新时间：2026-07-24

## 职责与边界

- 为文本、MCP 和 Agent 输入保存独立、不可变的原始快照。
- 导出 D1 中的 library、memory、图谱、来源和任务元数据。
- 导出 R2 原始内容与清洗结果，并提供可校验的清单。
- 定义软删除、恢复、专用 forget 和可选硬删除的清晰语义。
- 提供脱离当前 Cloudflare 资源后的重建说明和验证流程。

## 结构与数据流

```text
stage A input        -> immutable R2 snapshot -> asset/memory processing
stage B D1 + R2      -> manifest -> versioned JSON/NDJSON + files
export package       -> validation -> fresh resources -> rebuild vectors
forget request       -> scoped lifecycle policy -> D1/R2/vector cleanup
```

## 关键决策

1. 原始资产必须进入导出包，AI 派生内容可以选择重建。
2. 向量不作为唯一数据源，迁移时允许从文本重新生成。
3. 默认 forget 采用可恢复策略，硬删除需要显式选择和完整清理。
4. 导出格式不绑定 D1、R2 或 Vectorize 的内部表示。
5. M6 分阶段交付：阶段 A 在 M3 结构化记忆写入前完成不可变输入；阶段 B 在 M3 后完成
   导出、导入、重建和恢复演练。
6. M3 提供 `forget` 产品入口和用户操作，M6 定义跨存储删除、恢复、审计与重建规则。
7. 文本快照固定写入 `assets/<assetId>/raw/input.txt`，保留输入的 UTF-8 字节语义，
   包括首尾空格和换行形式；派生处理只清理读取后的工作副本。
8. D1 的 `rawR2Key` 只允许首次绑定。相同 key 可重复绑定，不同 key 视为冲突；
   已绑定但 R2 对象缺失时任务失败，禁止从可能变化的 D1 正文重建原文。
9. R2 写入成功、D1 绑定失败时，工作流重试会读取固定 key 的现有对象并补写指针。
10. Agent 记忆默认只保存选中的高密度内容。完整会话仅在用户显式归档时作为
    `recordKind=library` 进入导出范围。

## 当前实现

- 阶段 A 已完成。Web 文本、MCP `save_asset`、`remember` 和 `remember_agent` 共用
  `note_ingest_v1`，首个 `load_source` 步骤先保存或读取不可变 R2 快照，再进入
  清洗、摘要、图谱提取与索引。
- 文本快照成功后才绑定 D1 `rawR2Key`；R2 写入失败由现有 Queue 重投，完整输入在
  D1 中保留到快照成功。重处理只读取已归档原文，不覆盖固定 raw key。
- URL 和 PDF 继续使用各自的独立 R2 原始对象。
- 专用 `forget` 已按精确 scope/context 软删除 memory 并清理 chunk 向量；清理失败会
  明确回报 pending，D1 过滤仍立即阻止该记录进入检索。
- 专用 `restore_memory` 会恢复记录并从不可变 R2 快照重新进入处理流水线，重建
  chunk、Vectorize 和派生内容；排队失败会回滚到软删除状态。
- memory 更新保留每版不可变输入和版本链；`forget mode=hard` 只接受已经软删除的
  当前版本和完全匹配的 `confirmId`，随后清理同一 root 下全部版本。
- 硬删除先把版本链标为 `purge_pending_at` 并写 `deletion_audits`，再按 R2、chunk
  Vectorize、图 Vectorize、D1 的顺序幂等清理。审计只保存不可逆目标哈希、数量、
  阶段错误码和时间，不保存正文、对象 key 或明文目标 ID。
- L2 只删除全部 provenance 都来自该版本链的 statement/edge/entity；仍有其他来源的
  共享图记录会保留。外部存储失败时 D1 资产继续保留在不可检索的 purge pending
  状态，禁止恢复并允许重试。
- repository、blob 和 vector 端口为迁移保留了边界。
- 已有 fresh D1/Vectorize 初始化和 scope 向量运维脚本。
- 阶段 B 的数据包 v2 已落地：`data:export` 通过 Wrangler 分页读取 D1 正式引用的
  R2 key 和 Vectorize ID，生成逐业务表 D1 NDJSON、R2 文件、两个向量 NDJSON 与
  逐文件 SHA-256 清单；文件校验使用流式哈希，避免整文件载入内存。FTS5 是派生索引，
  恢复时从 `asset_chunks` 重建，规避 D1 原生导出不支持虚拟表的限制。
- `data:validate` 可完全离线校验清单、路径边界、文件大小和校验和。
- `data:restore` 只接受显式命名的隔离资源，恢复前检查 Vectorize metadata 索引，
  随后应用 migrations、按外键顺序导入 D1、重建 FTS、核对各表行数、上传 R2 并
  upsert 向量。`--resume` 仅在目标 D1 与清单逐表一致时放行。
- 隔离 D1 无需写入项目 `wrangler.jsonc`：恢复器读取目标 ID 后生成一次性配置执行
  migrations。业务数据按表、行数和字节分批写入；超过 D1 单条 SQL 上限的文本会按
  UTF-8 边界切片，先插入记录再逐段恢复，避免大体积 workflow 输出阻断整库导入。
- 完整步骤见 [数据导出与恢复 Runbook](../runbooks/数据导出与恢复.md)。
- BlobStore 当前仍只有按 key 写入和读取；数据包导出以 D1 正式引用为准，故不包含
  bucket 中未绑定的孤立对象。
- 生产 hard delete 验收和数据包 v2 fresh-resource 恢复演练已完成；验收临时 token、
  隔离 D1/R2/Vectorize 与本地导出包均已清理。

## 验证方式

- 阶段 A：单元测试覆盖精确保留原文、只读重处理、中断恢复、对象缺失和 R2 失败；
  Workers 集成测试通过真实 Miniflare D1 + R2 验证指针绑定、幂等与覆盖冲突。
- 阶段 B：用固定数据集执行“导出 -> 新资源导入 -> 重建向量 -> 查询核对”，并验证
  软删除、恢复和硬删除后的 D1、R2、Vectorize 一致性。
- 数据包核心单测覆盖结构、交叉引用、路径穿越、文件篡改、恢复续跑与目标污染保护。
- 硬删除单测覆盖确认、跨存储成功、外部失败审计和禁止恢复；Workers 集成测试覆盖
  整条版本链物理删除、独占 L2 清理、共享 L2 保留和无正文审计。
- 2026-07-24 真实演练：从生产资源只读导出 40 个文件和 24 个 R2 对象，在全新隔离
  D1/R2/Vectorize 中恢复；14 张业务表行数与清单一致，FTS 33/33、外键检查为空，
  R2 抽查 3 个对象校验和一致，两个向量索引为 24/100 条，3 个项目上下文的 9 个
  图向量查询全部遵守 `contextKey` 过滤。
- 2026-07-24 v2 最终演练：生产 hard delete 通过 D1/R2/Vectorize/L2 一致性检查；
  随后只读导出 41 个文件和 24 个 R2 对象，在全新隔离资源中恢复 15 张业务表，
  包含 3 条 `deletion_audits`。FTS 33/33、外键问题为 0，R2 抽查 3 个对象哈希一致，
  两个向量索引为 24/114 条，3 个项目 `contextKey` 原生过滤全部通过。

## 待扩展项

- M7 已把远端 migration 和部署后基础生产冒烟纳入自动发布；hard delete 和
  fresh-resource 恢复继续按发布核对周期执行真实演练，避免日常发布修改生产数据。
- 后续可增加 R2 孤立对象盘点；孤立对象不属于当前数据包的正式可恢复集合。

## 改动历史

- 2026-07-24：M7 自动发布已接入远端 migration 精确核验和基础生产冒烟；数据破坏型
  hard delete 验收与 fresh-resource 恢复保留为发布周期演练。
- 2026-07-24：完成生产 hard delete 和数据包 v2 最终 fresh-resource 恢复验收，关闭
  M6；全部验收临时资源和本地数据包已清理。
- 2026-07-24：实现可审计的 memory 版本链硬删除、跨 R2/Vectorize/D1 分阶段清理、
  purge pending 重试保护与共享 L2 保留；数据包升级为 v2 并纳入删除审计。
- 2026-07-24：完成生产只读导出与 fresh-resource 真实恢复演练；修复 FTS5 导出、
  隔离 D1 migration 绑定、D1 复合计数限制和超长文本分批恢复问题。
- 2026-07-24：实现数据包 v1、离线校验、受保护恢复、Vectorize 重建命令和恢复
  Runbook；真实 fresh-resource 演练与硬删除审计继续推进。
- 2026-07-24：M3-A2 补齐专用软删除、恢复重处理和 memory 不可变版本链；硬删除与
  跨存储审计继续留在阶段 B。
- 2026-07-24：取消 episode 与自动会话归档目标，明确只导出显式 library 和选中的
  memory 原文。
- 2026-07-24：完成阶段 A，为 Web 文本、MCP 和 Agent 输入增加不可变 R2 原始
  快照、首次条件绑定、中断恢复和真实 D1/R2 集成验证。
- 2026-07-23：拆分不可变快照与完整导出两个阶段，并记录当前原文和恢复缺口。
- 2026-07-22：建立模块归档，记录现有基础与尚未实现的交付边界。
